#include <iostream>
#define maxn 300005
using namespace std;
int  n,T,l,r,x.p;
char c;
int a[maxn],y[maxn];
int main()
{
    scanf("%d%d",&n,&T);
    while(T--){
        scanf("%c",&c);
        getchar();
        if(c==A){
            scanf("%d%d%d",&l,&r,&x);
            for(int i=l;i<=r;i++){
                y[i]++;
            }
        }
        else    if(c==B){
            scanf("%d",&p);

        }
        else    if(c==C){

        }
    }
    return 0;
}
